---
worker: __WORKER_NAME__
parent: plan.md
---

## Tasks

- [ ] <Concrete task 1. Imperative form. References file:line if relevant.>
- [ ] <Concrete task 2.>

## Reference files

<Files the worker must read before acting. Include line ranges where useful.>

- `<path/to/file>:<line-range>` — <what to look at>

## DoD for this worker

<Verifiable end-state for this worker only. The parent plan.md `## Verification`
aggregates these per-worker DoDs.>

- <One-line condition that is observable after this worker finishes.>

## Notes

<Optional: hidden constraints, gotchas, related memory entries the worker should
keep in mind. Link with [[memory-file-name]] if applicable.>
